# AGENTS

## Screenshots

Use Playwright with Firefox and save images under `./tmp`:

```bash
source ~/.nvm/nvm.sh
nvm use 22.20.0
npx -y playwright@latest screenshot --browser=firefox --device="Desktop Firefox" <url> <img-name>
```
